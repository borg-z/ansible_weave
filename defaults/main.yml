---

ansible_weave_paths:
  docker: /usr/bin/docker
  weave: /usr/local/bin/weave
  scope: /usr/local/bin/scope

ansible_weave_params:
  launch_wait: 3

weave_expose: true

# For creating custom networks
weave_name: test_net
weave_cidr: 10.20.0.0/16

weave_connect: false
weave_create_network: false
weave_expose_cidr: false
weave_install: false
weave_launch: false
weave_prime: false
weave_scope_install: false
weave_scope_launch: false
